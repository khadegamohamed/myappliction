<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.theproject22">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".sheets"></activity>
        <activity android:name=".vedioes" />
        <activity android:name=".lecture" />
        <activity android:name=".contact_supject" />
        <activity android:name=".moving_encouragement" />
        <activity android:name=".Table" />
        <activity android:name=".Encouragement" />
        <activity android:name=".subject_activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>